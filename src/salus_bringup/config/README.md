# Configuration profiles

This directory is intentionally empty. Future overrides belong in explicit
profiles such as `real/`, `real_wifi/`, `sim/`, and `sim_wifi/`; package-owned
defaults remain with the subsystem package.

